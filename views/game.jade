extends layout

block content
  canvas(width=800, height=600)
  script(src="/maps/#{map}.js")
  script.
    var game = new Game("#{blue}", "#{red}");

block scripts
  script(src="/javascripts/game.js")